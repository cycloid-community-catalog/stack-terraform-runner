# Configuration of the CI pipeline
name: 'terraform-runner'
canonical: 'stack-terraform-runner'
status: 'public'
description: 'Allow you to do simple terraform actions: plan, create, destroy'
keywords:
  - 'terraform-runner'
  - 'terraform'
technologies:
  - technology: terraform
    version: n/a

author: 'Cycloid'
image: 'https://raw.githubusercontent.com/cycloid-community-catalog/stack-terraform-runner/master/icon.png'
type: 'stack'
config:
  # Configuration of the CI pipeline
  pipeline:
    pipeline:
      path: 'pipeline/pipeline.yml'
    variables:
      path: 'pipeline/variables.sample.yml'
